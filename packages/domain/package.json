{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@repo/domain",
  "type": "module",
  "version": "0.1.0",
  "description": "Socket.io events maps which between client and server.",
  "scripts": {
    "build": "tsup",
    "check": "biome check --write ./src",
    "format": "biome format --write ./src",
    "lint": "biome lint --write ./src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "effect": "^3.12.0"
  },
  "devDependencies": {
    "@repo/biome-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "tsup": "^8.3.5"
  }
}
